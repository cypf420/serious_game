# 指南与反馈修复整合记录（2026-09-08）

## 基线与范围

以浊流之上0.5（整合前 HEAD 92afd29）为基线，将主目录 94651b9 上未提交的赴任指南合入；保留 0.5 中剧情、合同、人物与反馈修复。整合主目录最新反馈文档、教程维护说明及设计记录。历史设计文档保留其原始日期与适用版本，不作为当前机制说明。

主目录旧版统一“填写方案”，指南截图因此与修复工作区不一致。现按行动类型显示开始查阅、开始走访、开始约谈、开始沟通、发起会议；指南挂接同一套实际行动入口。移除指南中已删除的关键节点入口，地点说明改为核对固定地点，合同说明改为签订后扣除对应资源。普通走访/约谈仍保留预填的交流重点，本次没有另行改变该流程。

同时纳入已存在的未提交反馈修复：完整多人时间线、滚动保护、已收束多人会谈继续补充与显式结束、待决期间合规查档、服务端地点校验、变体标题、签约入口与库存显示、产品运行时移除 fake 网关（确定性替身仅供测试）。

独立提交 94651b9 的真实路线子集与诚实报告功能，经三方合并确认已包含于 0.5；两处冲突保留更新的剧情哈希映射与 Git 权威夹具，合并前后产品代码树无差异。

## 验证

- 前端 TypeScript 类型检查、ESLint 检查通过。
- 前端构建、资产验证（50 个剧情场景、24 个结局）通过。
- Node 单元测试 126/126 通过。
- 浏览器组件和指南测试 21 个用例：20 个通过，固定地点旧断言调整后剩余 1 个复测通过；另增九类行动没有“填写方案”按钮断言，单独复测通过。
- 后端核心定向测试 82/82 通过：待决查档、群组追问、反馈行动、合同结算、合同信息边界、统一行动、个人 AI 配置。
- 后端全量回归主动停止于约 31%，日志已输出 288 个通过标记；没有完成全量，不作全量通过声明。
- 历史内容哈希全套验证因逐文件还原 Git 数据较慢而中止，未声明通过；此次历史合并未改变加载器与测试代码。
- 本轮未调用付费真实模型 API、未部署线上服务；浏览器测试使用显式接口夹具，不能代替真实模型质量验收。
- GitNexus 检查返回 critical，但索引未命中前端组件且混入旧路径；未将该结果视为精确差异范围，另以 Git 实际差异与测试核对。

## 截图与版本使用

仓库截图：output/tutorial-reference/qa；用户原路径 E:/严肃游戏/output/tutorial-reference/qa 同步更新。截图为测试夹具下的 UI 验收，不是真实 API 会谈证据。

发布分支为浊流之上0.5。主目录在合入独立提交 94651b9 的历史后快进至同一提交，原未提交文件先备份到 E:/严肃游戏/output/integration-20260908/main-backup，避免后续从主目录启动旧版。不得再从历史工作区复制整份 GameShell.tsx 覆盖当前版本。
