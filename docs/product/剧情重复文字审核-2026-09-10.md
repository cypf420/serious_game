# 剧情重复文字审核（2026-09-10）

基线：9078ab0。审核对象为正式 v3 内容包及运行时结局修正，不使用模拟对话证明正式剧情。

## 文字检查

- 枚举 4,048 条固定可见文字，覆盖 90 日剧情、81 个决策的 688 个选项及 24 个主结局、95 个子结局。
- 对每条文字检查重复完整句（至少 12 字）及重复连续 60 字片段；应用现有结局修正后均未发现。
- 同一天不同 story beat 正文之间，未发现重复的至少 25 字完整句。
- 另检查 21 条运行时固定附记及办理反馈，未发现重复句。
- 检查以单条正文及同日剧情块为单位；互斥选项、不同结局之间的共同叙述不等于玩家重复阅读，不作机械删除。算法阈值之外的近义复述不承诺全部排除。

## 本次修正

1. 导出工具调用游戏共用的 `revise_ending_prose`，同步第 03、08、14、15、21 号主结局已有的措辞修正。重新生成 `outputs/当前游戏剧情全文_固定剧情校正版_2026-09-08.md`，该文件本次只有五段结局文字发生变化。
2. 全文输出测试逐项建立独立状态并满足该选项的事实、旗标、台账等条件；校验当前条件下的完整可见后果。它是序列化覆盖，不代表 688 个选项在同一条路线均可到达。证据解锁规则保持原样。
3. 纳入用户此前要求的普通剧情阅读方式回退：普通正文与回看恢复旧显示，结局卡片与结局分页保留。
4. 增加固定文字重复、同日剧情块重复、导出结局修正一致性的回归测试。

正式发布内容包、其哈希、《最终剧本.md》、结局判定规则以及结局之前的剧情正文均未修改。导出稿是全部分支注册清单，不是单局实绩；实际签约数仍由游戏根据本局台账呈现。

## 验证范围

- 后端剧情、文案修订、导出、结局展示及新增重复检查：508 项通过，2 项因 Windows 符号链接权限跳过（全部 `test_story*.py` 排除 `test_story_routes_v3.py`，加结局与重复专项）。
- `npm run check` 通过：139 项 Node 测试、59 项浏览器组件测试；175 项端到端用例仅完成发现，不计为运行通过。
- GitNexus 在真实 ASCII Git 工作树恢复索引，影响分析和变更检测均为 LOW；最终范围另经实际 Git 差异核对。
- 扩展检查曾进入 `test_story_routes_v3.py` 的隔离式 95 路线遍历并长时间等待。本次停止该遍历，清理本次启动的测试子进程；它不计为通过。本次不是完整 90 日游玩或所有路线可达性验收。
